# Aroma Café

Aplicativo Flutter com a tela inicial de uma cafeteria.

O código está organizado em `lib/main.dart`, `lib/screens/` e `lib/widgets/`.

## Como executar

```bash
flutter pub get
flutter run
```
